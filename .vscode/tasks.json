{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "Build and Test",
      "command": "cmake --build build && ctest --test-dir build",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [],
      "detail": "Build and Test task"
    }
  ]
}
